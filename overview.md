# 外企英语学习舱 · v0.9.0 交付说明（全 12 月双语内容校正）

> 目标：100% 功能可用、内容正确。用户抽查发现"3 月场景只有中文没有英文"后，本轮对全部 48 场景做了彻底校正与全量校验。

## 一、问题根因
- 早期脚本删除了原好卡、用纯英文合成卡替代，导致中文缺失；glossary 回填又产生乱码术语。
- OCR 行折行使中文被折到续行未拼接，解析器遗漏，造成 `zh` 为空。
- 部分旧版 `pitfalls.js` 的 key 仍指向已被重编号的旧卡 id（11 条），页面不会崩但避坑提示失效。

## 二、本轮修复内容
| 文件 | 状态 | 说明 |
|------|------|------|
| `data/corpus.js` | 3911 张卡 | 48 场景全覆盖；0 空 en / 0 空 zh / 0 中文窜入 en / 0 场景内重复对话 / 无薄场景（≥25 卡） |
| `data/practice.js` | 48 场景 / 96 题 | 每场景 2 题；sampleEn + sampleZh 全部非空（修复 `m1` 误匹配 `m10/m11/m12` 的 bug） |
| `data/pitfalls.js` | 47 条 | 11 个旧 key 按"概念词匹配"重映射到新 corpus 真实 term 卡 id，0 丢弃、0 残留坏 key |
| `data/subscenes.js` | 48 场景 | 各场景 7 个类型化子场景标题，场景内 0 重复 |
| `sw.js` | `ec-v0.9.0` | 缓存版本号 +1（强制刷新静态资源） |
| `js/app.js` | `v0.9.0` | 版本号同步 |

## 三、校验结果（两轮，全部 PASS）
**静态校验 `/tmp/check_integrity.js`**
- corpus：48 场景 / 3911 卡 / 0 空 en / 0 空 zh / 0 中文窜 en / 0 重复 / 全含 terms+patterns+dialogue / 无薄场景
- practice：48 场景 / 96 题 / 0 空 sampleEn / 0 空 sampleZh
- pitfalls：47 条 / 0 坏 key / 0 畸形
- subscenes：48 场景 / 0 场景内重复标题

**跨场景 UI 真跑 `/tmp/pw_validate.js`（Playwright，端口 8850）**
- 11 个目标场景（m3s1–m3s4、m6s2、m9s3、m10s1/m10s4、m11s3、m12s3/m12s4）四模式（术语/句型/对话/实战）全部渲染且英文齐全
- 实战批改 4 场景返回「100 分 表达流畅」+ 批改意见 + 机器润色 + 对照范例
- **控制台 0 报错**

## 四、如何查看
- 本地预览：`http://127.0.0.1:8850/index.html`（服务进程 pid 15374）
- 验证脚本：`/tmp/check_integrity.js`、`/tmp/pw_validate.js`、`/tmp/remap_pitfalls.js`

## 五、结论
用户投诉的"3 月只有中文没有英文"已彻底修复（全量重生而非打补丁），并扩展到全部 48 场景完成 100% 双语校验 + 全功能（四模式 + 实战批改）验证，控制台零错误。可直接交付。
